# Pagopa Test Generation Harness

이 저장소의 테스트 코드 생성·수정 요청에는 Codex 테스트 생성 하네스를 사용한다.

## 자연어 라우팅

사용자가 아래와 같은 요청을 하면 개별 작업을 바로 시작하지 말고
`spring-test-generation-orchestrator`를 먼저 사용한다.

- 테스트 코드 작성 또는 수정
- 정상·예외·경계값·회귀 테스트 설계
- 동시성 테스트 작성 또는 가독성 개선
- 기존 테스트 보강과 회귀 위험 확인

운영 코드 기능 구현만 요청했거나 테스트 실행 결과의 단순 설명만 요청한 경우에는 이
하네스를 자동으로 사용하지 않는다.

## 컨텍스트 경계

- 이 하네스 실행에서는 `ai/**`, `.claude/**`, `CLAUDE.md`, `GEMINI.md`를 지침이나
  참고 자료로 읽지 않는다.
- `build.gradle`, 운영 코드, 기존 테스트, 테스트 리소스와 사용자가 직접 제공한 자료를
  우선한다.
- 웹 조사가 필요하면 사용 중인 버전의 공식 문서와 프로젝트의 1차 자료를 우선한다.
- 작업 시작 시 `git status --short`로 기존 변경을 확인하고 사용자 작업을 보존한다.

## 필수 실행 규칙

1. 기존 코드와 가장 가까운 테스트를 읽어 실제 스타일과 테스트 수준을 확인한다.
2. `artifacts/02-test-cases.md`에 정상·예외·경계값·회귀 목록과 수정 범위를 작성한다.
3. 테스트 목록을 사용자에게 보여주고 명시적 승인을 받을 때까지 코드를 수정하지 않는다.
4. 승인 후 A 방식으로 대상 테스트 파일 안에서만 최소 구현·정리를 수행한다.
5. 구현자와 검증자를 분리하고, 테스트 실행 결과와 회귀 위험을 근거로 판정한다.
6. 변경 파일, diff, 테스트 결과와 설명을 사용자에게 보여주고 커밋 전에 멈춘다.
7. 사용자가 결과를 확인한 뒤 별도로 승인하기 전에는 `git add`, `git commit`, `git push`를
   실행하지 않는다.

## A 방식 변경 경계

- 새 테스트와 해당 테스트 파일 내부의 작은 helper 추출만 기본 허용 범위다.
- 운영 코드, `build.gradle`, 공용 Fixture, 공용 테스트 유틸리티, 다른 테스트 파일은 별도
  승인 없이 수정하지 않는다.
- 테스트를 통과시키려고 assertion을 약화하거나 예외를 삼키지 않는다.
- 운영 코드 결함이 드러나면 테스트를 실패 상태로 보존하고 원인과 선택지를 보고한다.
- B 방식인 공용 테스트 도구 추출은 별도 청사진, ADR, 사용자 승인과 회귀 검증 후 진행한다.

## 주요 위치

- 전체 진행표: `.agents/skills/spring-test-generation-orchestrator/SKILL.md`
- 작업 매뉴얼: `.agents/skills/`
- 역할 카드: `.codex/agents/`
- 실행 산출물과 상태: `artifacts/`

## 하네스 변경 이력

| 날짜 | 변경 내용 | 대상 | 사유 |
| --- | --- | --- | --- |
| 2026-09-03 | A 방식 테스트 코드 생성 하네스 초기 구성 | 전체 | 구조화·가독성·검증 부족 재발 방지 |
