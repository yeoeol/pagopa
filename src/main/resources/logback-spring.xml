<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <springProperty name="FLUENTD_HOST" source="logging.fluentd.host" defaultValue="localhost"/>
    <springProperty name="FLUENTD_PORT" source="logging.fluentd.port" defaultValue="24224"/>

    <!-- 1. 콘솔 Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%-5level) %clr(%logger{36}){cyan} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 2. 파일 롤링 Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/application.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- 3. Fluentd(Logstash) 전송용 Appender 추가 -->
    <appender name="FLUENTD" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>${FLUENTD_HOST}:${FLUENTD_PORT}</destination>

        <!-- 네트워크 단절 시 안정성을 위한 재시도 옵션 -->
        <keepAliveDuration>5 minutes</keepAliveDuration>
        <reconnectionDelay>10 seconds</reconnectionDelay>

        <!-- JSON 인코딩 -->
        <encoder class="net.logstash.logback.encoder.LogstashEncoder" />
    </appender>

    <!-- 개발 환경 (로컬 등) -->
    <springProfile name="local | dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
            <appender-ref ref="FLUENTD"/> <!-- 개발 중에도 EFK에서 확인 가능하도록 추가 -->
        </root>
        <logger name="org.hibernate.SQL" level="DEBUG"/>
    </springProfile>

    <!-- 운영 환경 (Prod) -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="FILE"/>
            <appender-ref ref="FLUENTD"/> <!-- 운영 환경에서도 EFK 연동 -->
        </root>
        <logger name="org.springframework" level="WARN"/>
    </springProfile>

    <!-- 기본 설정 (프로파일 지정 없을 때) -->
    <springProfile name="default">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FLUENTD"/>
        </root>
    </springProfile>

</configuration>